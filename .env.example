# Application
PORT=3000
NODE_ENV=production

# Database (Docker Internal)
DB_USER=whatsapp_user
DB_PASSWORD=whatsapp_password
DB_NAME=whatsapp_api

# If running outside Docker, use this:
# DATABASE_URL=postgres://whatsapp_user:whatsapp_password@localhost:5432/whatsapp_api
